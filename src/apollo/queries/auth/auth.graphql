mutation Signin($input: CredentialsInput!) {
  signin(input: $input) {
    userErrors {
      message
    }
    token
  }
}

mutation Signup($input: SignupInput!) {
  signup(input: $input) {
    userErrors {
      message
    }
    token
  }
}
